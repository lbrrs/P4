class Matching:

    def match_players(self, players):
        players.sort(key=lambda x: x.rank)
        for player in players:
            print(player.rank)

class Round:

    def
        lst_match=[]

